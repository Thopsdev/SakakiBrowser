# Sakaki Browser - Production Template
# Use a reverse proxy with TLS and keep bind on localhost unless required.

PORT=18800
SAKAKI_BIND=127.0.0.1
SAKAKI_ADMIN_TOKEN=change-me-very-long

# Browser backend (playwright recommended on servers)
SAKAKI_BACKEND=playwright
SAKAKI_BROWSER=chromium
# SAKAKI_ALLOW_NO_SANDBOX=1      # legacy fallback only (avoid in production)

# Vault storage (optional override)
# VAULT_SOCKET=/var/run/sakaki/vault.sock
# VAULT_FILE=/var/lib/sakaki/vault.enc

# Secure lane allowlist
SAKAKI_SECURE_ALLOWED_DOMAINS=example.com,login.example.com
SAKAKI_SECURE_ALLOW_SUBDOMAINS=1
# SAKAKI_SECURE_ALLOW_HTTP=1         # dev only
# SAKAKI_SECURE_ALLOW_PRIVATE=1      # dev only
# SAKAKI_SECURE_ALLOW_SENSITIVE=1    # not recommended

# Vault browser allowlist (defaults to secure allowlist if unset)
# SAKAKI_VAULT_BROWSER_ALLOWED_DOMAINS=example.com,login.example.com
# SAKAKI_VAULT_BROWSER_ALLOW_SUBDOMAINS=1

# Vault proxy guard
SAKAKI_PROXY_REQUIRE_ALLOWLIST=1
# SAKAKI_PROXY_ALLOW_HTTP=1          # dev only
# SAKAKI_PROXY_ALLOW_PRIVATE=1       # dev only

# Public lane
# SAKAKI_PUBLIC_ALLOW_HTTP=1         # dev only

# Remote View (disabled by default)
# SAKAKI_REMOTE_VIEW=1
# SAKAKI_REMOTE_VIEW_MAX_SESSIONS=1
# SAKAKI_REMOTE_VIEW_TTL_MS=900000
# SAKAKI_REMOTE_VIEW_BLOCK_SENSITIVE=1
